<div class="season_capitles_container">
  <table>
    <tr>
      <th>Temporada <%= season %></th>
    </tr>
    <% capitles.each do |capitle| %>
      <tr>
        <td>
          <%= link_to "#{capitle.order}- #{capitle.title}",
                      serie_capitle_path(capitle.serie.title, capitle.season, capitle.order),
                      :class => "capitle_link" %>
        </td>
      </tr>
    <%  end %>
  </table>
</div>
